<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.easterlyn</groupId>
    <artifactId>Easterlyn</artifactId>
    <packaging>pom</packaging>
    <version>2.0</version>

    <properties>
        <bukkit-api-version>1.15</bukkit-api-version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.build.testSourceDirectory>${project.basedir}/test/java</project.build.testSourceDirectory>
        <project.build.testResourceDirectory>${project.basedir}/test/resources</project.build.testResourceDirectory>
    </properties>

    <repositories>
        <repository>
            <id>md_5</id>
            <url>http://repo.md-5.net/content/groups/public/</url>
        </repository>
        <repository>
            <id>aikar</id>
            <url>https://repo.aikar.co/content/groups/aikar/</url>
        </repository>
        <repository>
            <id>destroystokyo-repo</id>
            <url>https://repo.destroystokyo.com/repository/maven-public//</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>net.md-5</groupId>
            <artifactId>bungeecord-chat</artifactId>
            <version>1.16-R0.1</version>
            <scope>provided</scope>
        </dependency>
        <!-- Module core uses server jar, also needs to specify API for dependency resolution order. -->
        <!--suppress MavenDuplicateDependenciesInspection -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>1.16.1-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>co.aikar</groupId>
            <artifactId>acf-paper</artifactId>
            <version>0.5.0-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.jetbrains</groupId>
            <artifactId>annotations</artifactId>
            <version>19.0.0</version>
            <scope>provided</scope>
        </dependency>
        <!-- Tests -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <modules>
        <module>core</module>
        <module>chat</module> <!-- depends: core -->
        <module>afk</module> <!-- depends: core, chat -->
        <module>captchas</module> <!-- depends: core -->
        <module>discord</module> <!-- depends: core, chat -->
        <module>effects</module> <!-- depends: core -->
        <module>kitchensink</module> <!-- depends: core, captcha -->
        <module>machines</module> <!-- depends: core, captcha -->
        <module>sleepvote</module> <!-- depends: core -->
        <module>spectators</module> <!-- depends: core -->
        <module>villagers</module> <!-- depends: core -->
        <module>netherportals</module> <!-- depends: core -->
    </modules>

    <build>

        <finalName>${project.name}</finalName>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>

        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>timestamp-property</id>
                        <goals>
                            <goal>timestamp-property</goal>
                        </goals>
                        <configuration>
                            <name>timestamp</name>
                            <pattern>MM/dd/yyyy@HH:mm</pattern>
                            <locale>en_US</locale>
                            <timeZone>America/New_York</timeZone>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.2.4</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                    <!--<forceJavacCompilerUse>true</forceJavacCompilerUse>-->
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.0.0-M5</version>
            </plugin>

            <plugin>
                <groupId>ch.mfrey.maven.plugin</groupId>
                <artifactId>copy-maven-plugin</artifactId>
                <version>1.0.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <resources>
                                <resource>
                                    <id>copy</id>
                                    <directory>${project.build.directory}</directory>
                                    <workOnFullPath>true</workOnFullPath>
                                    <replaceExisting>true</replaceExisting>
                                    <includes>
                                        <include>*.jar</include>
                                    </includes>
                                    <excludes>
                                        <exclude>original-*.jar</exclude>
                                    </excludes>
                                    <paths>
                                        <path>
                                            <from>${project.build.directory}</from>
                                            <!--
                                             ~ Property should be defined in build command, i.e.:
                                             ~ mvn clean package -Dproject.build.finaldir=/minecraft/testserver/plugins/
                                            -->
                                            <!--suppress UnresolvedMavenProperty -->
                                            <to>${project.build.finaldir}</to>
                                        </path>
                                    </paths>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
