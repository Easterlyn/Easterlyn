/**
 * 
 */
package co.sblock.Sblock;

/**
 * This interface marks a class that contains inner classes that handle commands.
 * 
 * Each command listener in a class implementing this interface must have the annotation <code>@CommandHandler(name)</code>
 * above the class declaration, where <code>name</code> is the name of the command being handled.
 * @author FireNG
 */
public interface CommandListener
{

}
